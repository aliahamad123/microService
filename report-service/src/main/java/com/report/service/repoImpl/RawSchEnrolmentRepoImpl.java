package com.report.service.repoImpl;

//@Service
public class RawSchEnrolmentRepoImpl{
	
/*	@Autowired
	HibernateTemplate hibernateTemplate;
	
	public RawSchoolEnrolment fetchEnrolmentDetailsBySchId(Integer schoolId){
		//RawSchoolEnrolment rawSchoolEnrolment = null;
		String query = "from RawSchoolEnrolment where schoolId = ?";
		@SuppressWarnings("unchecked")
		List<RawSchoolEnrolment>  rawSchoolEnrolment = (List<RawSchoolEnrolment>) getHibernateTemplate().find(query, schoolId);
		if(rawSchoolEnrolment != null && rawSchoolEnrolment.size()>0) {
			return rawSchoolEnrolment.get(0);
		}*/
		//return null;
		
		
	//}

}
