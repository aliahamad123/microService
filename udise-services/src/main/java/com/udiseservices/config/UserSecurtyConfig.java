package com.udiseservices.config;

import org.springframework.beans.factory.annotation.Autowired;
/*import org.springframework.security.config.annotation.authentication.builders.AuthenticationManagerBuilder;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;*/
import org.springframework.security.crypto.password.PasswordEncoder;

import com.udiseservices.repository.UserDetailServiceImpl;

//@EnableWebSecurity
public class UserSecurtyConfig{

	/*
	 * @Autowired UserDetailServiceImpl userDetailServiceImpl;
	 * 
	 * @Override protected void configure(HttpSecurity http) throws Exception {
	 * System.out.println("Security"); // TODO Auto-generated method stub
	 * http.authorizeRequests().antMatchers("/**").hasAuthority("USER").and().
	 * httpBasic(); }
	 * 
	 * @Override protected void configure(AuthenticationManagerBuilder auth) throws
	 * Exception { auth.userDetailsService(userDetailServiceImpl); }
	 */

}
