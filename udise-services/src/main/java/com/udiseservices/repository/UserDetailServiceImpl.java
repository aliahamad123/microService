package com.udiseservices.repository;

import org.springframework.beans.factory.annotation.Autowired;
/*import org.springframework.security.core.userdetails.User;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;*/
import org.springframework.stereotype.Service;

import com.udiseservices.models.UserMaster;
import com.udiseservices.servicesImpl.UserDeatilsImpl;

@Service

//public class UserDetailServiceImpl implements UserDetailsService {
public class UserDetailServiceImpl{

	/*
	 * @Autowired UserDeatilsImpl userDeatilsImpl;
	 * 
	 * @Override public UserDetails loadUserByUsername(String username) throws
	 * UsernameNotFoundException { // TODO Auto-generated method stub
	 * System.out.println("Ali LoadUser By user Name"); UserMaster usermaster =
	 * userDeatilsImpl.getUserDetail(username); UserDetails user =
	 * User.withUsername(usermaster.getUsername()).password(usermaster.getPassword()
	 * ) .authorities("USER").build(); return user; }
	 */

}
